<div class="container">
  <div class="card">
    <div class="card-header">
      <h1>빌려드려요</h1>
    </div>

    <div class="card-body">
      <%= render 'form', gife: @gife %>
      <%= link_to 'Back', gives_path %>
    </div>
  </div>
</div>
